# Обзор проекта RAG системы

## Что было создано

Полный пайплайн для преобразования Markdown документов в работающую RAG (Retrieval-Augmented Generation) систему.

## Структура проекта

```
ai_helper/
│
├── 📄 markdown_to_rag.py          # Шаг 1: Преобразование Markdown → JSON/JSONL
├── 📄 rag_pipeline_full.py        # Шаг 2: Полный пайплайн обработки
├── 📄 example_rag_usage.py        # Примеры использования RAG системы
│
├── 📁 rag_pipeline/               # Модули RAG системы
│   ├── __init__.py
│   ├── data_cleaner.py            # ✅ Очистка данных
│   ├── text_chunker.py            # ✅ Разбиение на чанки
│   ├── embeddings.py              # ✅ Генерация эмбеддингов
│   ├── vector_db.py               # ✅ Работа с векторными БД
│   └── rag_system.py              # ✅ Интеграция с LLM
│
├── 📄 requirements_rag.txt        # Зависимости
├── 📄 QUICKSTART.md               # Быстрый старт
├── 📄 README_RAG.md               # Документация базового преобразования
└── 📄 README_RAG_FULL.md          # Полная документация
```

## Функциональность

### ✅ 1. Очистка данных (`data_cleaner.py`)

- **Удаление дубликатов** - находит и удаляет повторяющиеся записи
- **Нормализация текста** - унифицирует формат, убирает лишние пробелы
- **Исправление опечаток** - базовая нормализация знаков препинания
- **Унификация формата** - приводит вопросы и ответы к единому виду

**Использование:**
```python
from rag_pipeline.data_cleaner import DataCleaner

cleaner = DataCleaner()
cleaned = cleaner.clean_records(records, remove_duplicates=True)
```

### ✅ 2. Разбиение на чанки (`text_chunker.py`)

- **Смысловое разбиение** - по предложениям, с сохранением контекста
- **Настраиваемый размер** - можно задать размер чанка и перекрытие
- **Автоматическое определение** - разбивает только длинные тексты

**Использование:**
```python
from rag_pipeline.text_chunker import TextChunker

chunker = TextChunker(chunk_size=500, chunk_overlap=50)
chunked = chunker.chunk_records(records)
```

### ✅ 3. Генерация эмбеддингов (`embeddings.py`)

- **OpenAI** - высококачественные эмбеддинги (платно)
  - `text-embedding-3-small` (1536 dim)
  - `text-embedding-3-large` (3072 dim)
  
- **Sentence Transformers** - бесплатные локальные эмбеддинги
  - `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` - мультиязычная модель (рекомендуется, без Xet Storage)
  - Поддержка русского языка и множества других языков

**Использование:**
```python
from rag_pipeline.embeddings import create_embedding_generator

# OpenAI
generator = create_embedding_generator(
    provider="openai",
    model="text-embedding-3-small"
)

# Sentence Transformers (рекомендуемая модель без Xet Storage)
generator = create_embedding_generator(
    provider="sentence_transformers",
    model_name="sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)
```

### ✅ 4. Векторные БД (`vector_db.py`)

- **Pinecone** - облачный сервис, простой в использовании
- **Qdrant** - самостоятельный хостинг, open-source

**Использование:**
```python
from rag_pipeline.vector_db import create_vector_db

# Pinecone
db = create_vector_db(
    provider="pinecone",
    api_key="...",
    index_name="rag-index"
)

# Qdrant
db = create_vector_db(
    provider="qdrant",
    url="http://localhost:6333",
    collection_name="rag_collection"
)
```

### ✅ 5. RAG система (`rag_system.py`)

- **Семантический поиск** - находит релевантные фрагменты
- **Генерация ответов** - через LLM (OpenAI, Anthropic)
- **Фильтрация** - по категориям, разделам и другим метаданным

**Использование:**
```python
from rag_pipeline.rag_system import RAGSystem

rag = RAGSystem(
    vector_db=vector_db,
    embedding_generator=embedding_generator,
    llm_provider="openai",
    llm_model="gpt-3.5-turbo"
)

# Полный цикл: поиск + генерация
result = rag.ask("Как проверить баланс?", top_k=5)
print(result['answer'])
```

## Процесс работы

### Шаг 1: Преобразование Markdown
```bash
python markdown_to_rag.py --input test_asks.md --output rag_data.jsonl
```

**Результат:** Структурированные данные в JSON/JSONL формате.

### Шаг 2: Полная обработка
```bash
python rag_pipeline_full.py \
    --input rag_data.jsonl \
    --output rag_processed.json \
    --embedding-provider sentence_transformers \
    --upload-db \
    --db-provider pinecone \
    --pinecone-index "rag-index"
```

**Что происходит:**
1. Загрузка данных
2. Очистка (удаление дубликатов, нормализация)
3. Разбиение на чанки (для длинных текстов)
4. Генерация эмбеддингов
5. Загрузка в векторную БД

### Шаг 3: Использование RAG системы
```python
# Поиск и генерация ответов
result = rag.ask("Ваш вопрос", top_k=5)
```

## Преимущества решения

✅ **Модульность** - каждый компонент можно использовать отдельно  
✅ **Гибкость** - поддержка разных провайдеров (OpenAI, Sentence Transformers)  
✅ **Масштабируемость** - работает с большими объемами данных  
✅ **Простота** - понятный API, подробная документация  
✅ **Готовность к production** - обработка ошибок, логирование  

## Примеры использования

### Пример 1: Только очистка данных
```python
from rag_pipeline.data_cleaner import DataCleaner

cleaner = DataCleaner()
cleaned = cleaner.clean_records(records)
stats = cleaner.get_statistics(cleaned)
```

### Пример 2: Только поиск (без LLM)
```python
query_embedding = embedding_generator.generate([query])[0]
results = vector_db.search(query_embedding, top_k=5)
```

### Пример 3: Полный RAG цикл
```python
result = rag.ask(
    "Как заблокировать карту?",
    top_k=5,
    filter_dict={"category": "Безопасность"}
)
```

## Требования

### Минимальные (для базовой обработки)
- Python 3.7+
- pandas
- sentence-transformers

### Полный функционал
См. `requirements_rag.txt`

## Следующие шаги

1. **Запустите базовое преобразование:**
   ```bash
   python markdown_to_rag.py
   ```

2. **Обработайте данные:**
   ```bash
   python rag_pipeline_full.py --input rag_data.jsonl
   ```

3. **Настройте векторную БД** (Pinecone или Qdrant)

4. **Используйте RAG систему** для поиска и генерации ответов

## Документация

- **QUICKSTART.md** - быстрый старт
- **README_RAG.md** - базовое преобразование Markdown
- **README_RAG_FULL.md** - полная документация пайплайна
- **example_rag_usage.py** - примеры кода

## Поддержка

При возникновении проблем проверьте:
1. Правильность API ключей
2. Установлены ли все зависимости
3. Соответствие размерности векторов модели и БД

